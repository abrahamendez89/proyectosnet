using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Herramientas.Conversiones
{
    public class ORMFormats
    {
        public void ObtieneDataGrid()
        {
            //List<_Producto> productos = productoCtrl.FiltrarXAplicacion(idMarca, idLinea, idMotor, año, idLineaProducto);
            //if (productos != null && productos.Count >= 1)
            //{
            //    dg_productos.DataSource = productos.Select(o => new { ID = o.Id, CODIGO = o.Lg_codigoBarras, NOMBRE = o.St_nombreProducto, DESC = o.St_descripcionProducto, STOCK = o.In_actualStock, UBICACION = o.St_ubicacion }).ToList(); ;
            //}
        }
    }
}
